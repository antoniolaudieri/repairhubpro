-- Add timestamp columns for each workflow status
ALTER TABLE public.repair_requests
ADD COLUMN IF NOT EXISTS quote_sent_at TIMESTAMP WITH TIME ZONE,
ADD COLUMN IF NOT EXISTS quote_accepted_at TIMESTAMP WITH TIME ZONE,
ADD COLUMN IF NOT EXISTS awaiting_pickup_at TIMESTAMP WITH TIME ZONE,
ADD COLUMN IF NOT EXISTS picked_up_at TIMESTAMP WITH TIME ZONE,
ADD COLUMN IF NOT EXISTS in_diagnosis_at TIMESTAMP WITH TIME ZONE,
ADD COLUMN IF NOT EXISTS waiting_for_parts_at TIMESTAMP WITH TIME ZONE,
ADD COLUMN IF NOT EXISTS in_repair_at TIMESTAMP WITH TIME ZONE,
ADD COLUMN IF NOT EXISTS repair_completed_at TIMESTAMP WITH TIME ZONE,
ADD COLUMN IF NOT EXISTS ready_for_return_at TIMESTAMP WITH TIME ZONE,
ADD COLUMN IF NOT EXISTS at_corner_at TIMESTAMP WITH TIME ZONE,
ADD COLUMN IF NOT EXISTS delivered_at TIMESTAMP WITH TIME ZONE;